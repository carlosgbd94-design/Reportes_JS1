const fs=require('fs');let code=fs.readFileSync('main.js','utf8');code=code.replace('if (bioHint) bioHint.textContent = \Ventana de captura cerrada.\;\\n      const icon = bioDayAlert.querySelector(\.bioDayIcon\);', 'if (bioHint) bioHint.textContent = \Ventana de captura cerrada.\;\\n      bioDayAlert.className = \IntegratedHint tone-bad mb-4 flex\;\\n      const icon = bioDayAlert.querySelector(\.bioDayIcon\);');fs.writeFileSync('main.js',code);
